{
  "dependencies": {
    "gh-pages": "^4.0.0",
    "parcel": "^2.8.0",
    "typescript": "^4.8.4"
  },
  "engines": {
    "node": ">=12"
  },
  "scripts": {
    "build": "parcel build src/index.html --no-source-maps --public-url https://bbrk24.github.io/gender-square",
    "start": "parcel serve src/index.html --open",
    "clean": "rm -R dist/* && gh-pages-clean",
    "deploy": "gh-pages -d dist"
  }
}
